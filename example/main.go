package main

// func Login(ctx *alpha.Context) {
// 	ctx.response.Write([]byte("login请求成功"))
// }

// func Register(w http.ResponseWriter, r *http.Request) {
// 	w.Write([]byte("register请求成功"))
// }

func main() {
	// h := alpha.NewHTTP(alpha.WithHTTPServerStop(nil))
	// go func() {
	// 	err := h.Start(":8080")
	// 	if err != nil && err != http.ErrServerClosed {
	// 		panic("启动失败")
	// 	}
	// }()
	// err := h.Stop()
	// if err != nil {
	// 	panic("关闭失败")
	// }

	// h := alpha.NewHTTP()
	// h.GET("/login", Login)
	// h.POST("/register", Register)
	// err := h.Start(":8080")
	// if err != nil {
	// 	panic(err)
	// }
}
